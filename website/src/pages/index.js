import React from "react";
import Layout from "../components/Layout";

const HomePage = () => {
  return (
    <Layout>
      <section>
        <h1>Server-side Swift made fast, flexible and easy.</h1>
        <button>Get Started</button>
      </section>
    </Layout>
  );
};

export default HomePage;
